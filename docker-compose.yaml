version: '3'

services:
  kweli-os:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:   # Binds volume to enable working through docker locally
      - type: bind
        source: .
        target: /app/
    ports:
      - 8000:8000
    environment: 
      PORT: 8000